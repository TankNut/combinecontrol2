FLAG.Name = "Unnamed Character Flag"
FLAG.Team = TEAM_UNCONNECTED

FLAG.Health = 100
FLAG.ArmorValue = 0 -- Todo: Remove, placeholder to not break gamemode code

FLAG.BloodColor = BLOOD_COLOR_RED

FLAG.Loadout = {}

function FLAG:OnSpawn(ply)
end
